// alert("test");
// const stars = document.querySelectorAll('#star');
// alert(stars);
const lista = document.querySelectorAll('#star');

for (let i = 0; i < lista.length; i++) {
    lista[i].style.color = "red";
}